# CLAUDE.md — Me.Stella 작업 규약

이 문서는 Claude Code가 이 저장소에서 작업할 때 따르는 유일한 규약이다.
모든 코드 작업 전에 이 문서를 전제로 하며, 규약과 충돌하는 요청을 받으면 진행 전에 충돌 사실을 지적한다.

---

## 1. 프로젝트 개요

- 제품: Me.Stella — 2022 개정 수학과 교육과정(초1~고등 미적분Ⅰ) 기반 게임형 수학 학습 서비스 (교육부 고시 제2022-33호 [별책 8])
- 체제: 1인 개발(허준성) / 6개월 내 v1.0 출시 / 인프라 비용 $0
- v1 콘텐츠 범위: Stage 4~6 (중학교, 24단원, 360문제)
- 기준 문서(SSOT): 제품 요구사항은 PRD v0.4, 학습 체계·단원은 커리큘럼 v0.2. 커리큘럼 문서는 학습 내용의 단일 진실 원천이며, 코드와 커리큘럼이 충돌하면 커리큘럼 문서가 우선한다.

## 2. 아키텍처 — 하이브리드 (변경 금지, 변경은 게이트에서만)

- 웹 셸(web/): Next.js + TypeScript + Tailwind. 웹 셸 안에 Unity 임베드 페이지가 있고, 로그인 후 JWT를 JS interop 브릿지로 Unity에 전달한다.
- 게임 클라이언트(unity/client/): Unity 6.3 LTS · C# · 2D(Built-in Render Pipeline) · Web(WebGL) 빌드.
- API: web/ 안의 Next.js Route Handlers가 담당하며, 모든 비즈니스 로직은 서버 전용이다.
- 데이터: Supabase (PostgreSQL · Auth · Storage).

화면 소유권 원칙은 "긴 텍스트와 폼은 웹, 놀이와 연출은 Unity"이다.
- 웹 셸 담당: 랜딩, 인증, 온보딩, 개념 학습(KaTeX), 대시보드, 오답노트, 프로필.
- Unity 담당: 학습 지도, 문제 풀이 플레이, 정오답·완료 연출, 게임화 요소 전반.
- 서버 담당: 채점, 마스터리, 단원 잠금·해제 판정 (예외 없음).

## 3. 절대 금지 사항 (위반 = 즉시 작업 중단)

1. 정답·해설의 사전 노출 금지. 문제 조회 API 응답에 answer, solution 필드를 절대 포함하지 않는다. 해설은 채점 응답에만 포함한다.
2. 판정 로직의 클라이언트 구현 금지. 채점·마스터리 점수·단원 해제를 웹/Unity 클라이언트에서 계산하는 코드를 작성하지 않는다. 클라이언트는 표시만 한다. Unity는 변조 가능한 환경으로 간주한다.
3. 클라이언트의 Supabase 직접 질의 금지. 모든 데이터 접근은 Next.js API를 경유한다. RLS는 2차 방어선일 뿐이다.
4. 시크릿 접근 금지. .env 계열 파일을 읽거나, 출력하거나, 커밋하지 않는다. 키 값이 필요한 코드는 process.env 참조로만 작성한다.
5. 마이그레이션·시드의 자동 실행 금지. 파일 작성까지만 하고, 실행은 사람이 명시적으로 승인·수행한다.
6. 성취기준 코드 생성 금지. 교육과정 성취기준 코드([9수01-XX] 등)를 추정하거나 지어내지 않는다. 커리큘럼 문서에 검증·기입된 코드만 사용하며, 미기입 상태면 빈 값으로 두고 사람에게 알린다.
7. 게임화 콘텐츠 창작 금지. 세계관·스테이지 테마명·캐릭터·보상 수치·도전과제·연출 컨셉은 기획자(허준성) 전담 영역이다. 게임화 기획서(W6) 확정 전에는 무테마 플레이스홀더만 사용하고, 확정 후에는 기획서 문구를 그대로 구현한다. 임의로 채워 넣는 친절은 위반이다.
8. Unity 씬(.unity)·프리팹(.prefab) 파일 직접 편집 금지. YAML 병합 파손 위험이 있다. 씬 구조 변경은 사람이 에디터에서 수행하고, Claude Code는 C# 스크립트·에디터 스크립트만 담당한다.
9. JWT 영구 저장 금지. 토큰은 브릿지로 전달받아 메모리에서만 사용하며, localStorage·PlayerPrefs에 저장하지 않는다.

## 4. 개발 규약

- 커밋: 기능 단위로 소분한다. 대량 생성 후 일괄 커밋은 금지한다. 메시지 형식은 "type: 요약" (type은 feat / fix / chore / docs / test / content).
- TypeScript: strict를 유지한다. any 사용 시 사유 주석을 단다.
- 입력 검증: 모든 API 입력은 Zod 스키마로 검증한다.
- 테스트: 채점·마스터리·잠금 로직은 단위 테스트를 필수로 동반한다(Vitest). 이 로직들은 테스트 없는 PR을 만들지 않는다.
- C#: Unity 스크립트는 단일 책임 원칙을 따르고, API 호출은 ApiClient 단일 모듈로만 한다(분산 호출 금지).
- 주석·문서는 한국어, 코드 식별자는 영어로 작성한다.
- 환경은 Windows / PowerShell 기준이다. 셸 명령 예시는 PowerShell 호환으로 제시한다.

## 5. 학습 시스템 핵심 수치 (PRD 4.4 — 구현 시 이 값 사용)

- 마스터리: 단원 점수 0~100, 초기값 0, 85점 도달 시 단원 완료.
- 정답 가점: 힌트 미사용 +20 / 힌트1 +16 / 힌트2 +10 / 힌트3 +5.
- 오답 감점: -10 (하한 0).
- 복습 간격: 3일 → 7일 → 21일 (실패 시 3일로 리셋).
- 위 수치는 전부 제안값이며, 변경은 PRD CHANGELOG 기록과 함께만 한다.
- topic_id 형식은 "{stage}-{seq 2자리}"이다 (예: "4-03"). 전체 10 Stage 72 Unit.

## 6. 저장소 구조

- CLAUDE.md: 본 문서.
- web/: Next.js 웹 셸 + API (create-next-app 생성 구조 유지).
- unity/client/: Unity 프로젝트 (에디터 6000.3.19f1).
- content/: (예정) 문제·개념 원고 JSON과 검증·시드 스크립트.
- docs/: (예정) PRD·커리큘럼 문서 사본.
- 생성 도구가 만든 구조를 임의로 재배치하지 않는다.

## 7. 현재 단계 (매주 갱신)

- 현재: W1 — 환경 구축 진행 중 (Day 2까지 완료).
- 다음 관문: Gate 0 (W2~W3) — Unity 기술 스파이크 Go/No-Go. 스파이크 기간에는 검증 목적의 최소 구현을 우선하되, 버릴 코드라도 금지 사항(3장)은 동일하게 적용한다.
- 미설치 의존성(Day 3 예정): prisma, @supabase/supabase-js, zod, katex.

## 8. 계획 대비 변경 사항 (Deviation Log)

- [~] 2026-07-10 create-next-app을 개별 질문 응답 대신 권장 기본값으로 생성(template: app-tw). TS·Tailwind·ESLint·App Router 포함 확인됨. src/ 디렉터리 유무는 생성 결과를 확인해 본 항에 추기할 것.
- [~] 2026-07-10 Unity 프로젝트 경로를 unity/에서 unity/client/로 확정(프로젝트명 client, 템플릿 2D Built-In Render Pipeline).
- [~] 2026-07-10 PDF 2-B 3번(필수 패키지 설치)은 미수행 — Day 3(3-A)로 이월.
- [~] 2026-07-10 CLAUDE.md를 Day 2 계획(4항목 초판) 대비 전체 규약판으로 상향 작성(기획자 지시).

## 9. 문서 유지 규칙

- 아키텍처·규약이 바뀌면 코드보다 먼저 이 문서를 갱신한다.
- 변경 표기는 [+] 추가 / [~] 변경 / [-] 삭제와 날짜를 함께 쓴다.
- 이 문서와 PRD가 충돌하면 PRD가 우선하되, 충돌 발견 즉시 사람에게 보고한다.


## 문서 관리 규칙

- PRD·커리큘럼은 이중 SSOT이며, 갱신 시 즉시 docs/에 커밋한다.
  로컬(Downloads·바탕화면)에만 존재하는 상태를 금지한다.
- 문서 경로: docs/prd/, docs/curriculum/, docs/plan/, docs/reference/
- 문서 우선순위: 교육부 고시 원본 PDF > 커리큘럼 > PRD > 기타 내부 문서
- 작업 기계는 데스크탑 1대. 노트북 사본은 읽기 전용 백업이며 커밋 금지.